# Conexión de solo lectura (rol_motor) a la base compartida
URL_BASE_DATOS="postgresql://rol_motor:Tu_clave@localhost:5433/red_cuidar"

# Redis para la caché del mapa de calor
URL_REDIS="redis://localhost:6379"

# Clave compartida con la API Node (misma CLAVE_SERVICIO_INTERNO)
CLAVE_SERVICIO_INTERNO="Tu clave de servicio interno"

# Segundos de vida de cada mapa de calor en caché
TTL_CACHE_SEGUNDOS=300
